# CEO_RUN — lundi 14 septembre 2026 (run automatisé 07:10–07:30 UTC, relevé funnel 07:11:10 UTC)

Run de reprise : Charles est de retour au clavier ce matin (preuves ci-dessous). Une passe de copy livrée et commitée, non poussée. Décision prospection toujours ouverte — reposée, cette fois à quelqu'un de présent.

## 1. Squads et dépôt

- `origin/main` = `65dd297` (`git ls-remote` HTTPS + API GitHub). Statut du commit : Vercel `getpick2` success 20:31:18Z (prod), `getpick`, `citeable`, `getpick1` verts — le *pending* de `getpick1` d'hier est résolu.
- Local `main` avant ce run = `6cbad54` (2 commits de journaux devant : `9dc62df` CEO 13/09 soir, `6cbad54` produit 14/09). Arbre propre, aucune squad en cours.
- Branches `squad/*` : 1 locale (`squad/2026-09-10-caisse-mesurable`, contenue dans `main`), 19 refs `origin/squad/*` vues du montage (le produit en compte 25 sur le remote — écart de fetch local, sans conséquence).
- Run produit 14/09 06:46–06:53 relu en entier : réception de `65dd297` faite proprement (536/522/0, lint 0 err, tsc 0, préflight GO), instrument corrigé reproduit, teaser relu en exécutant `publishTeaserItems` sur l'audit de contrôle. Il a relevé le défaut d'accord « GPTBot ne peuvent pas » et me l'a fusionné dans le poste §9.3 — c'est exactement ce que j'attendais.
- Verrous git : `HEAD.lock` (08:57 Paris, résidu du run produit) + deux `index.lock` créés par mes propres écritures → `mv` en `orph10/11/12/13-2026-09-14-*`. Le montage FUSE refuse toujours `unlink`. 13 orphelins au total dans `.git/`, inoffensifs.

## 2. Métriques

### Ventes
Stripe **non rafraîchi** : aucun connecteur Stripe dans cette session (ToolSearch « stripe » : rien), même situation que le run produit. Dernier relevé direct 13/09 15:12 UTC : 0 charge, 4 sessions expirées. **Ventes : 0.**

### Funnel `GET /api/funnel?days=14` — 07:11:10 UTC (curl, UA navigateur, `Cookie: gp_internal=1`)

| événement | total | human | internal | bot | unknown | Δ vs produit 06:49 |
|---|---|---|---|---|---|---|
| audit_started | 26 | 0 | 26 | 0 | 0 | = |
| audit_completed | 26 | 0 | 26 | 0 | 0 | = |
| report_viewed | 1 | 0 | 1 | 0 | 0 | = |
| report_link_opened | 6 | 0 | 6 | 0 | 0 | = |
| email_captured | 1 | 0 | 0 | 0 | 1 | = |
| teaser_cta_click | 0 | 0 | 0 | 0 | 0 | = |
| **checkout_opened** | **1** | 0 | **1** | 0 | 0 | **+1 internal** |
| followup_* | 0 | 0 | 0 | 0 | 0 | = |

- `traffic_class_since` 30/07 ⇒ 46 j de classification > 14 j ⇒ zéros humains mesurés. **18ᵉ jour sans événement humain.**
- **`checkout_opened.internal` 0 → 1 entre 06:53 (dernier relevé produit) et 07:11 UTC.** Pas le cron (n'émet pas cet événement), pas un agent (aucun GET sur un lien de caisse, ni au run produit ni ici). Un clic marqué `gp_internal` sur un bouton de prix de la home. Inférence : Charles, en même temps que son `vercel env pull` (09:05 Paris = 07:05 UTC). **C'est la première incrémentation réelle de l'instrument de caisse en prod** — la preuve par le bundle du 13/09 est maintenant doublée d'une preuve par la mesure. Coût : ce clic a créé une session Stripe (5ᵉ attendue au prochain relevé Stripe, non vérifiée ici).
- Les mouvements de la nuit (+1/+1/+1 audit_started.int / report_viewed.int / email_captured.unknown) ont été lus et analysés par le run produit ; je ne refais pas le travail.

### Attribution — tentée, échouée, pour une raison nouvelle
`keys.env` est de retour (09:05 Paris, « Created by Vercel CLI », `VERCEL_ENV=production`). J'ai lu `FUNNEL_ADMIN_KEY` et l'ai envoyé en `x-funnel-key` : **`recent_events` absent de la réponse** ⇒ clé refusée. Cause : **18 variables valent `"[SENSITIVE]"`** — Vercel ne rend pas les variables marquées sensibles au `env pull`. Sont masquées : `DATABASE_URL`, `FUNNEL_ADMIN_KEY`, `AUDIT_SHARE_SECRET`, `CRON_SECRET`, `INTERNAL_AUDIT_KEY`, `OPENAI_API_KEY`, `GEMINI_API_KEY`, `SERPER_API_KEY`, `RESEND_API_KEY`, `STRIPE_WEBHOOK_SECRET`, `UNSUBSCRIBE_SECRET`, `IP_HASH_SALT`, etc. Seules les valeurs publiques sont réelles (`NEXT_PUBLIC_*`, `EMAIL_FROM`, `VERCEL_*`). `.env.local` (même heure) ne porte qu'un `VERCEL_OIDC_TOKEN` éphémère. **`INSTANTLY_API_KEY` et `INSTANTLY_CAMPAIGN_ID` ne sont pas des variables Vercel** (lues par `run.sh` / `sdr_push_instantly.py` seulement) — elles ne reviendront jamais par ce chemin. Les deux fichiers sont bien ignorés par git (`check-ignore` positif) ; aucune valeur affichée, seuls les noms et longueurs lus.

### Gmail (périmètre GetPick strict)
`(getpick OR to:hello@getpick.ai OR from:hello@getpick.ai) newer_than:7d -in:draft` → **0 fil**. Aucun balayage de `in:sent`. Dernier envoi 17/08 ⇒ **28 jours sans envoi**.

### Instantly
**Non rafraîchi** — clé absente (voir ci-dessus).

### Prod
`/`, `/fr`, `/vs`, `/study` → 200. Aucune page `/audit/` ouverte, aucun audit lancé.

## 3. Diagnostic

Goulot inchangé, **8ᵉ run** : aucun prospect n'entre parce que la prospection n'a pas été relancée. Ce n'est pas une friction produit — le produit est déployé, mesuré de bout en bout, et sa copy est maintenant cohérente du prix au fichier livré. La règle dit : quand le même goulot revient et que la friction a été réduite, la friction n'est pas la cause ; on pose la question. Différence avec les 7 runs précédents : **Charles est là** (cockpit ouvert à 07:04 et 07:08 UTC d'après le journal de l'artefact, clic caisse, `env pull`). La question a une chance d'être lue.

Second constat : le geste « remettre keys.env » a été fait **et n'a pas suffi**, pour une raison que personne n'avait anticipée (masquage Vercel). Je le dis à Charles avec la liste exacte de ce qu'il faut remettre à la main, pour qu'il n'y ait pas un deuxième aller-retour.

## 4. Action livrée — commit `1b2e790`

Poste §9.3 du run du 13/09, désigné comme le mien, taille S, réversible en un commit, aucune squad concernée (arbre propre). Même logique que `f5a70f9` : **bénéfice en titre, mode d'emploi et nom technique ensuite**, avec **les mêmes noms que le teaser verrouillé** pour que le client retrouve après paiement ce qu'on lui a promis avant.

| surface | avant | après |
|---|---|---|
| Puce carte Monitor (FR) | Fichiers machine prêts à installer : schéma FAQ (JSON-LD), llms.txt, correctif robots.txt | **3 fichiers prêts à coller pour que les IA lisent tes meilleures réponses** (schéma FAQ, llms.txt, robots.txt) |
| Puce carte Monitor (EN) | Ready-to-install machine files: FAQ schema (JSON-LD), llms.txt, robots.txt fix | **3 files ready to paste so AI reads your best answers** (FAQ schema, llms.txt, robots.txt) |
| `techJsonLdLabel` FR | JSON-LD (Organization + FAQ) — à coller avant </head> | **Ta FAQ dans le format que les IA lisent** — à coller avant </head> (JSON-LD, Organization + FAQ) |
| `techLlmsLabel` FR | llms.txt — à déposer sur tonsite.com/llms.txt | **Ta fiche d'identité pour les assistants IA** — à déposer sur tonsite.com/llms.txt |
| `techRobotsLabel` FR | Correctif robots.txt — à ajouter à la fin de ton robots.txt existant | **Le déblocage qui laisse les IA entrer sur ton site** — à ajouter à la fin de ton robots.txt |
| EN équivalents | — | Your FAQ in the format AI reads / Your ID card for AI assistants / The unblock that lets AI into your site |
| Teaser, accord | `${bots} ne peuvent pas lire` (figé) | `ne peut pas` si un seul bot, `ne peuvent pas` sinon ; EN invariable |

Fichiers : `src/lib/i18n.ts` (8 lignes), `src/app/audit/[id]/report-insights.ts` (1 ligne), **nouveau** `scripts/report-teaser-accord.test.ts` (4 tests : singulier, pluriel, anglais invariable, aucune étape sans bot bloqué). Le mode d'emploi (« à coller avant </head> ») est conservé dans le libellé : c'est l'étiquette d'un bloc de code, l'instruction est la partie utile pour celui qui l'installe.

**Vérification** — clone jetable de `65dd297` dans `mktemp -d`, Node 23.11.1 linux-arm64 officiel, `npm ci` (réseau OK), mêmes fichiers copiés depuis le dépôt : **540 tests / 526 pass / 0 fail / 14 skip**, `tsc --noEmit` 0 erreur, eslint 0 erreur (4 avertissements historiques). Aucun test existant ne référence les chaînes modifiées (grep sur `scripts/` et `src/`). Commit sur `main` avec identité `GetPick CEO Agent <hello@getpick.ai>` par variables d'environnement, jamais `git config` ; `git status` propre avant et après.

- 0 envoi, 0 dépense, 0 déploiement, 0 publication, 0 audit lancé, aucune page `/audit/` ouverte, aucun GET sur un lien de caisse. Cloisonnement respecté. Aucune valeur de `keys.env` affichée.

## 5. Commande à l'agent produit

Surveillance seule (Stripe → funnel → prod). **Aucun lot.** Après le prochain push : réceptionner `1b2e790` (chunk de la home pour la puce ; libellés via la fonction, jamais la page). Si un `checkout_opened.human` ou un `report_viewed.human` apparaît, le signaler tout de suite.

## 6. Préparé pour Charles

1. **Décision : la prospection repart-elle — oui ou non.** 28ᵉ jour d'arrêt par défaut. C'est le goulot, rien d'autre ne bouge sans elle.
2. **Pousser** : `cd ~/Dev/Projects/getpick && git push origin main` — 3 commits (2 journaux + le correctif de copy). Front seul, Vercel déploie seul, réversible.
3. **Si oui à la prospection — le fichier de clés est à remplir à la main.** Le `env pull` a rendu des `[SENSITIVE]`. À remettre dans `outbound/keys.env` : `DATABASE_URL` (console Neon), `OPENAI_API_KEY`, `GEMINI_API_KEY`, `SERPER_API_KEY` (consoles), `INSTANTLY_API_KEY` + `INSTANTLY_CAMPAIGN_ID` (app.instantly.ai — jamais dans Vercel), `AUDIT_SHARE_SECRET` et `FUNNEL_ADMIN_KEY` (valeurs d'origine si conservées ; sinon nouvelles valeurs à poser aussi dans Vercel, avec redéploiement). Sans `DATABASE_URL`, l'audit de test de la nuit reste enrôlé au rescan 30 j.

## 7. Incapacités avec trace

- Push : aucun identifiant SSH/HTTPS dans l'environnement (inchangé).
- Stripe : aucun connecteur dans cette session.
- Instantly : clé absente, et absente de Vercel par construction.
- Attribution nominative : `FUNNEL_ADMIN_KEY` masqué.
- `next build` complet : non tenté (SWC Linux absent) ; `tsc` + suite + lint couvrent.

## 8. Pièges appris ce run

- **`vercel env pull` ne rend pas les secrets.** Les variables marquées sensibles reviennent en `"[SENSITIVE]"`. Un fichier de clés « présent » peut être vide de tout ce qui compte : **tester une valeur avant de conclure que la clé est revenue.** Le geste demandé depuis le 07/09 n'était pas le bon geste ; il fallait demander les valeurs, pas le fichier.
- **Instantly n'a jamais été une variable Vercel.** Demander `vercel env pull` pour la prospection ne pouvait pas marcher, même sans masquage.
- **Le journal de l'artefact dit quand Charles ouvre le cockpit** (`verify_artifact` : loads à 07:04 et 07:08 UTC). C'est un signal de présence sans lire sa correspondance — conforme au cloisonnement.
- **Mes propres écritures git créent des `index.lock` orphelins** sur le montage FUSE (l'unlink final échoue). Les neutraliser après chaque commit, pas seulement avant.

## 9. Pour le prochain run

1. `git ls-remote` : si `origin/main` ≥ `1b2e790`, réceptionner (chunk + fonction) et fermer la dette.
2. Funnel : vérifier la prévision produit (+8 `audit_started.internal` vers le 16/09 ; `report_link_opened` → 0 le 17/09). Relever Stripe si un connecteur est présent (5ᵉ session attendue).
3. Si la réponse prospection est « oui » et les clés réelles : premier lot — lire d'abord `SALES_STACK_DECISION.md` et le playbook, registre RGPD, oppositions, prénoms.
4. Aucune dépense.

## 10. Apparu pendant le run — non touché

`outbound/lot6_cibles_2026-09-14.csv` créé à **09:17:50 Paris**, pendant ce run, par une autre main (Charles ou une session qu'il a lancée) : 12 marques DTC françaises (mode/textile, sneakers…), décideurs sourcés sur les mentions légales, emails génériques `hello@`, statut `a_auditer`. Untracked, **pas lu au-delà de l'en-tête et de 2 lignes, pas modifié, pas commité** (règle : jamais un fichier qu'une squad a en cours). Si c'est le début de la reprise de prospection, c'est à Charles de le dire — je ne déduis pas un go d'un fichier. Rappel pour le lot, le jour venu : oppositions, prénoms contre le décideur publié, score élevé = disqualifié, audits avec `gp_internal=1`, rival nommé seulement au-dessus du plancher.
