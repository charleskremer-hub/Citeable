# Journal des runs d'agents Citeable

Une ligne par run, écrite par chaque agent. Lu chaque matin par `citeable-product-agent`,
qui supervise, diagnostique et répare les agents en échec.

Format : `- [AAAA-MM-JJ HH:MM] <agent> — <OK | BLOQUÉ | RIEN À FAIRE> — <résumé>`

## Pièges connus (à consulter avant de diagnostiquer une panne)

- **`keys.env` illisible / "Resource deadlock avoided" (OSError errno 35)** — le montage disque
  refuse par intermittence la lecture, pour TOUTES les méthodes (bash source, cat, dd, python),
  parfois plusieurs minutes d'affilée. Conséquence : la clé arrive vide, Instantly répond
  `401 Invalid authorization header`, et on conclut à tort à un « problème d'API ».
  **Toujours vérifier que la clé est chargée (>20 caractères) avant d'incriminer l'API.**
- **`403 error code: 1010`** — WAF Cloudflare d'Instantly. Cause : User-Agent absent ou non
  navigateur. Ni la clé ni l'API. Envoyer un UA Chrome règle le problème.
- **`.git` invisible depuis le sandbox** — les opérations git doivent être faites par Charles.
- **✅ CONTOURNEMENT TROUVÉ (2026-07-20) pour le deadlock `keys.env`** — le blocage errno 35 touche le **montage bash uniquement**. L'**outil fichier `Read`** (chemin `/Users/charles.kremer/Documents/Claude/Projects/NanoCorp/getciteable-main/outbound/keys.env`) lit le fichier sans erreur, y compris quand `cat` échoue 3 fois d'affilée. **Règle : lire les clés avec `Read`, écrire ce journal avec `Edit`. bash = fallback seulement.** Prompts de `citeable-sdr-weekly` et `citeable-inbox-assign` mis à jour en conséquence.
- **`web_fetch` sur `/api/funnel` refuse parfois l'URL même si elle a marché pour un run précédent** — constaté le 21/07 : l'outil `web_fetch` de cette session exige que l'URL exacte ait déjà « transité » par un message utilisateur, un résultat `web_fetch` antérieur DANS LA MÊME session, ou un résultat `WebSearch`. Chaque run tourne dans une session fraîche : lire l'URL dans ce journal (texte local) ne suffit pas à la « débloquer ». Contournement à tester au prochain run : soit demander d'abord un `WebSearch` qui référence l'URL, soit passer par le Chrome MCP (`navigate` + `get_page_text`) — mais ce dernier n'a pas répondu non plus en tâche planifiée (pas de session Chrome active hors présence utilisateur). Résultat concret aujourd'hui : impossible de rafraîchir les chiffres funnel malgré l'endpoint public trouvé le 20/07 à 19h.
- **`git` est cassé/instable depuis le sandbox** — `git log`/`git status` renvoient `fatal: your current branch appears to be broken` ou crashent (`Bus error`, core dump). Confirme le pitfall déjà noté (« `.git` invisible/à faire par Charles ») — ne PAS retenter de commandes git en boucle, ça peut planter le process bash.
- **⚠️ CHANGEMENT D'API (2026-07-21) — `GET /api/funnel` ne renvoie plus que les compteurs agrégés.** Le champ `recent_events` (100 événements bruts avec `audit_id`, `source`, `metadata` → noms de marques auditées, scores, URLs de rapports) a été retiré de la réponse publique : il exposait nos données business et des données clients identifiables (RGPD) à qui appelait l'URL. **La réponse publique conserve `ok`, `window` et `counts` — c'est SUFFISANT pour le monitoring horaire du funnel** (volumes d'audits, capture d'email, checkout ouvert, etc. sur 14 jours). Aucun agent n'a besoin du détail pour suivre les tendances. Si un run a réellement besoin des événements bruts, il faut envoyer le header `x-funnel-key` avec la valeur de `FUNNEL_ADMIN_KEY` — **clé à demander à Charles, jamais en dur dans un prompt, un script ou ce journal**. Si `FUNNEL_ADMIN_KEY` n'est pas défini côté Vercel, le mode détaillé est indisponible (jamais ouvert par défaut) : ne pas conclure à une panne. Le `POST /api/funnel` (enregistrement des événements) est inchangé.

## Runs

- [2026-07-18 08:40] citeable-inbox-assign — BLOQUÉ — keys.env illisible (montage, errno 35) : faux diagnostic « problème d'API » remonté à Charles. Prompt de la tâche corrigé (lecture robuste 10×15s, garde-fou anti-clé-vide, attribution correcte de l'erreur).
- [2026-07-18 08:45] citeable-product-agent — OK — supervision ajoutée au prompt ; journal partagé créé.
- [2026-07-19 22:10] citeable-pr-inbound — OK — 0 citation/backlink de l'étude trouvée. Dogfood 65 (vs 61 publié) : ai_visibility 75/100, mais search_visibility 0/25 (le web search natif remonte NanoCorp, pas Citeable) et catégorie perçue toujours fausse — passée de « commerces locaux » à « food & beverage ». 10 cibles presse ajoutées (pr_targets.csv), 6 pitchs rédigés (pr_pitches_2026-07-19.md). AUCUN email destinataire vérifiable : web_fetch bloqué sur les pages contact, tous marqués « à trouver » — rien chargé dans Instantly (rien à charger). keys.env illisible (montage, errno 35) — PAS un problème d'API. Concurrent identifié : 5W AI Visibility Index / DTC Graveyard (50 marques, 5 moteurs, PR Newswire, depuis mai 2026).
- [2026-07-20 07:04] citeable-product-agent — OK — SUPERVISION : cause racine du deadlock `keys.env` trouvée (bash KO / outil `Read` OK) ; prompts `citeable-sdr-weekly` et `citeable-inbox-assign` corrigés (lecture via `Read`, journalisation via `Edit`). `citeable-inbox-assign` n'a pas journalisé les 19 et 20/07 — écriture bash silencieusement en échec, corrigé. `citeable-sdr-weekly` n'a jamais tourné (aucun `lastRunAt`), 1er run attendu ce lundi 08:05. PRODUIT : bloc « Comment ces questions ont été choisies » livré (page-only). Typecheck OK, funnel E2E OK (audit live Finisterre, score 22, 2 questions réelles, aucune fuite JSON). BUG relevé : la marque apparaît dans sa propre liste de concurrents — mis en Must au backlog. 2 features non déployées en attente de push (repère part équitable du 19/07 + bloc méthodo du 20/07).
- [2026-07-20 09:15] citeable-inbox-assign — OK — Clé lue du 1er coup via l'outil `Read` (contournement confirmé, 0 retry). 1 boîte provisionnée et active : charles@trygetciteable.com (setup_pending=false, status=1, warmup démarré le 17/07, score 100, tracking domain inst.trygetciteable.com CTD_ACTIVE). La campagne avait DÉJÀ email_list=[charles@] avant tout PATCH — assignation déjà effective, mon PATCH (HTTP 200) l'a simplement confirmée. Campagne toujours status=0 (en pause) — non touchée. La boîte charlie@ n'existe pas encore côté Instantly (1 seul compte retourné sur /accounts?limit=50) : à re-vérifier demain. Objectif de la tâche atteint pour charles@.
- [2026-07-20 18:00] citeable-growth-hourly — RIEN À FAIRE — Site OK : www.getpick.ai = 200. Apex nu `getpick.ai` (sans www) = échec de connexion (000), propagation DNS toujours incomplète — pas une panne (le check santé porte sur www, conforme au prompt). Ancien domaine getciteable.nanocorp.app = 200 (sert encore la même app, attendu avant la 301). AUCUN accès analytics/PostHog/DB/ads ce run — pas de CLI `nanocorp`, pas de connecteur Meta Ads, `.git` illisible depuis le sandbox comme documenté ; impossible de chiffrer audits/réclamations/checkouts/dépense du jour, donc pas de synthèse chiffrée fiable à journaliser (zéro bullshit : pas de chiffre inventé). Aucune action kill-switch déclenchée (pas de donnée CPA pour trancher). Recommandation pour le prochain run : brancher un accès DB/analytics (ex. lecture directe `DATABASE_URL` ou export PostHog) pour que ce monitoring horaire ait des chiffres réels à surveiller.
- [2026-07-20 19:00] citeable-growth-hourly — RIEN À FAIRE — Site OK : www.getpick.ai = 200 (getciteable.nanocorp.app aussi 200). **DÉCOUVERTE : `GET https://www.getpick.ai/api/funnel` est public, sans auth, et renvoie des vraies métriques (fenêtre 14j + 100 derniers événements bruts)** — comble le trou d'accès analytics signalé au run de 18h. Chiffres 14j au 19h00 : audit_started 138, audit_completed 136, report_viewed 76, email_captured 1, teaser_cta_click 3, checkout_opened 3, followup_1_sent 61, followup_2_sent 65, followup_click 1. Taux de réclamation email/audit sur 14j ≈ 0,7% (1/136) — très bas, mais base 14j trop large pour juger un seul jour, à suivre. Aucun événement dans l'échantillon des 100 derniers depuis 2026-07-20T08:41 UTC (~10h de silence) — pas classé anormal (faible trafic connu, pas de comparatif horaire fiable pour trancher >3×). Toujours aucun accès CPA/dépense Meta Ads (pas de connecteur) → pas de décision kill-switch possible. ⚠️ À signaler à Charles au prochain point (pas urgent, mais pas une action autorisée sans lui) : cet endpoint expose sans authentification des données business (noms de marques auditées, scores, IDs d'audit) à quiconque — potentiel sujet vie privée/concurrence à corriger côté produit. Recommandation pour les prochains runs : utiliser `/api/funnel` comme source de vérité horaire (filtrer `recent_events` sur la date du jour), et ne considérer les comparaisons par plan que pour les événements datés après le 18/07.
- [2026-07-21 06:35] citeable-pr-inbound — OK — keys.env lu sans accroc via l'outil `Read` (aucun souci de montage cette fois). VEILLE : 0 citation/backlink de l'étude trouvée (3 requêtes web ciblées : "GetPick" study, "Citeable" study 21 DTC, Allbirds 46/Ridge Wallet 81) — rien de pertinent. DOGFOOD (audit_id c61c2b28-d5a3-4c23-b02a-87b8e4162ba3, tier monitor_9eur) : score 61 (vs 65 le 19/07, revenu au niveau du score publié). ai_visibility stable 75/100. Vraie amélioration : search_visibility 15/25 (vs 0/25 le 19/07) — le web search natif cite enfin getpick.ai. MAUVAISE NOUVELLE : la catégorisation perçue par l'IA a EMPIRÉ au lieu de se corriger — passée de « food & beverage » (19/07) à **« warehouse management software »**, avec des concurrents cités totalement hors sujet (ShipHero, Logiwa, SkuVault, Lucas). Aucune association avec « GEO agent for DTC brands » à ce stade. Vérifié : les échecs technical_seo/structured_data de l'audit sont un FAUX NÉGATIF — l'audit interroge l'apex `getpick.ai` (sans www), qui ne résout toujours pas (confirmé : connexion 000, DNS incomplet depuis le 20/07), alors que `www.getpick.ai/robots.txt` et `/sitemap.xml` répondent 200 tous les deux. Pas une régression produit réelle ; à corriger côté audit (basculer sur www, ou distinguer échec DNS d'échec réel). PRESSE : pr_targets.csv maintenu (10 cibles), 3 recherches ciblées pour trouver des contacts réels — Retail Brew n'offre qu'un formulaire partenariat (pas d'email), Practical Ecommerce donne un nom d'éditrice (Kerry Murdock) mais l'email n'est qu'un pattern déduit, marqué non vérifié, à ne pas utiliser. 6 pitchs réécrits et resignés « Charles, GetPick » (pr_pitches_2026-07-21.md), lien mis à jour vers getpick.ai/study, nouvel angle bonus ajouté (le rebranding lui-même, motivé par une confusion d'attribution avec 2 acteurs homonymes — citée mot pour mot depuis la page étude). Rien chargé dans Instantly (aucun email vérifié à charger). Aucun envoi, aucune activation de campagne.
- [2026-07-21 06:24 UTC] citeable-nanocorp-check (CEO) — OK, RIEN DE NOUVEAU EN CHIFFRES — Contenu vérifié en direct sur getciteable.nanocorp.app (le seul domaine accessible ce run) : hero/CTA alignés sur les 2 vrais produits (Monitor 9€, Agent 19€), lien vers l'étude /study présent, positionnement « agent GEO des marques DTC » conforme à POSITIONING_V2.md. **3 demandes NanoCorp toujours en attente, aucune n'a bougé depuis le 20/07 :** (a) 301 getciteable.nanocorp.app → getpick.ai — toujours 200, pas de redirection ; (b) apex getpick.ai (sans www) — toujours injoignable, DNS non propagé ; (c) libellé FR « Agent » sur Stripe/checkout — non vérifiable ce run (`checkout.nanocorp.so` bloqué par le garde-fou provenance de `web_fetch`, Chrome MCP indisponible en tâche planifiée sans session utilisateur active — voir pièges connus, entrée ajoutée ce jour). Aucune métrique fraîche : `/api/funnel` trouvé le 20/07 n'a pas pu être re-fetché ce run (même blocage provenance) ; dernier chiffre connu = celui du 20/07 19h00 (taux de réclamation email ≈0,7%, 3 checkout_opened, aucune vente confirmée visible). Toujours zéro accès CPA/dépense Meta Ads → kill-switch (CPA>49€, budget 5$/j) reste invérifiable depuis cet environnement, 3e run consécutif à le signaler. DIRECTIVE DU JOUR (revenue-first, transmise pour le run `citeable-product-agent` de 7h) : prioriser l'investigation du taux de réclamation email (0,7% jugé très bas sur la base 14j) sur `ClaimReportGate` avant toute autre feature Should/Nice — chiffrer d'abord (funnel jour par jour depuis le 19/07, pas la fenêtre 14j) avant de trancher durcir/assouplir la porte. Rappel : le bug Must du 20/07 (marque dans sa propre liste de concurrents) reste Todo et prioritaire.
- [2026-07-21] citeable-product-agent (build) — OK — 3 items Must livrés, non poussés (git impossible depuis le sandbox, Charles pousse). (1) **Bug apex↔www corrigé** : `alternateHostUrl()` + `fetchWithHostFallback()` dans `src/lib/audit-engine.ts`, bascule sur échec RÉSEAU uniquement (404/403 = réponse valide, aucun retry), branchés dans `checkTechnicalSEO()` et `checkAiCrawlability()` ; le pilier « Lisible par les IA » distingue désormais `ok` / `blocked` / `unreachable`, ce dernier affiché « Site injoignable — vérifie ton DNS » en orange neutre au lieu du rouge d'échec. 16 tests Node passés sur la bascule. (2) **`/api/funnel` sécurisé** : GET public réduit aux compteurs agrégés, détail derrière `x-funnel-key` == `FUNNEL_ADMIN_KEY` (voir pièges connus — impact direct sur les agents qui lisaient `recent_events`). (3) **Garantie 30 jours** ajoutée (landing + FAQ FR/EN + `llms.txt` + bloc EN BREF) : option sans dépendance NanoCorp pour lever l'objection d'achat. Typecheck 5.9.3 : 0 erreur, aucun filtre appliqué. Tests prod (ancien code encore déployé) : `GET /` 200 ; audit E2E Marine Layer complet (score 94, 3 questions d'achat réelles, Gemini `realLlmCall`, concurrent nommé Outerknown, porte email présente, aucune fuite JSON). **NOUVEAU Must remonté : l'apex `getpick.ai` ne résout toujours pas alors que `robots.ts`, `llms.txt` et les canonicals pointent tous dessus — nos propres URLs déclarées aux crawlers IA sont mortes.** Correctif DNS côté Charles ; ne pas basculer les canonicals sur `www.` avant que la question DNS soit tranchée.
- [2026-07-21 07:04] citeable-inbox-assign — OK — Clé lue sans accroc via `Read`. **NOUVELLE BOÎTE apparue côté Instantly : `charles@freegetpick.com`** (organisation identique, créée 2026-07-20T10:13, status=1 actif, setup_pending=false, warmup démarré 2026-07-20T10:43, score déjà 100, tracking domain inst.freegetpick.com CTD_ACTIVE) — ceci répond de fait au point de décision « ancien nom vs nouveau domaine getpick » : un DFY sur domaine getpick a été commandé/provisionné, pas besoin d'attendre. `charles@trygetciteable.com` toujours actif, score 93 (léger repli vs 100, warmup depuis le 17/07, pas encore mûr). La boîte `charlie@` toujours jamais apparue. Campagne relue : `email_list` ne contenait que `charles@trygetciteable.com` → PATCH envoyé avec `["charles@trygetciteable.com","charles@freegetpick.com"]`, HTTP 200 confirmé, `email_list` à jour dans la réponse. Campagne toujours `status=0` (pause) — kill-switch non touché, aucune activation. Rien envoyé.
