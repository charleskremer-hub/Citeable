# CEO_RUN — dimanche 13 septembre 2026 (relevé 15:12 UTC, puis intervention de Charles ~17:00 UTC)

Run de surveillance, interrompu par un retour terrain de Charles sur la page de rapport. Une correction produit livrée et commitée.

## 1. Squads et dépôt

- `origin/main` = `1d338cb` (`git ls-remote`, lu ce jour). `main` local = **`f5a70f9`** après mon commit. **7 commits devant, toujours rien de poussé.**
- Branches `squad/*` : inchangées, toutes sur origin sauf `squad/2026-09-10-caisse-mesurable` (locale, contenue dans `main`).
- **Deux verrous git orphelins** trouvés dans `.git` : `HEAD.lock` daté du **12/09 16:05** (résidu de la session Agent Produit bloquée sur `list_projects`, vue au run d'hier) et un `index.lock` du jour. Le montage FUSE refuse `unlink` : neutralisés par `mv` (`orphelin-2026-09-13-*`, `orphelin2-2026-09-13-*`). Ils se recréent vides à chaque écriture git et se re-neutralisent de la même façon — ils n'empêchent pas `git push`.
- `AGENT_RUNS.md` : relu, **aucun doublon** de la session produit du 12/09 (risque signalé hier, levé).

## 2. Métriques

### Stripe `acct_1TzBMoCZqJGb866f` livemode (lu avant le funnel)
- `/v1/charges` : **liste vide**. `/v1/checkout/sessions` : les **4 mêmes** sessions expirées impayées (27/08 ×3, 31/07), aucune nouvelle. **0 € depuis l'origine, 17 jours sans session nouvelle.**

### Funnel `GET /api/funnel?days=14`, 15:12 UTC (curl, UA navigateur, `Cookie: gp_internal=1`)
| événement | total | human | internal | bot | unknown |
|---|---|---|---|---|---|
| audit_started | 25 | 0 | 25 | 0 | 0 |
| audit_completed | 25 | 0 | 25 | 0 | 0 |
| report_viewed | 0 | 0 | 0 | 0 | 0 |
| report_link_opened | **6** | 0 | 6 | 0 | 0 |
| email_captured / teaser / checkout / followups | 0 | 0 | 0 | 0 | 0 |

- `traffic_class_since = 2026-07-30T07:03:49Z` ⇒ fenêtre de classification 45 j > fenêtre 14 j ⇒ les zéros sont **mesurés**, pas une absence de données.
- **Premier mouvement en 3 runs** : `report_link_opened` passe de 7 à 6. Ce n'est pas une entrée, c'est une **sortie par le bord** de la fenêtre glissante — un clic interne du 30/08 vient d'avoir 14 jours. Rien n'est entré. **17ᵉ jour sans aucun événement humain.**
- Prod : `/`, `/fr`, `/vs`, `/study` → 200 ; `/api/audit-status` sans paramètre → 400 ; home servie **sans** `checkout_opened` (attendu, correctif non déployé). **Aucune page `/audit/` ouverte** — y compris celle que Charles a partagée (lue par le code, jamais par la page).

### Gmail (périmètre GetPick strict)
- `(getpick OR to:hello@getpick.ai OR from:hello@getpick.ai) newer_than:7d -in:draft` → **0 fil**. Aucun balayage de `in:sent`. Dernier envoi 17/08 ⇒ **27 jours sans envoi**.

### Instantly
- **Non rafraîchi** : `keys.env` toujours absent (re-vérifié). Aucune campagne active depuis le 17/08.

## 3. Diagnostic

Goulot inchangé, 7ᵉ run : **aucun prospect n'entre**, faute de décision sur la prospection. Je ne redemande pas une troisième fois dans le vide — la question reste ouverte dans le report, en une ligne.

Mais le retour de Charles ouvre un **second constat, lui vérifiable sans prospect** : la page de rapport parle machine à un public qui ne l'est pas. Ça ne change pas le goulot (personne n'entre), ça change ce qui arrivera quand quelqu'un entrera. C'est exactement le genre de dette qu'on paie à froid plutôt qu'en pleine campagne.

## 4. Action livrée — commit `f5a70f9`

Retour de Charles, texto : la page `/audit/<id>` est « incompréhensible pour un client lambda », en citant les deux lignes verrouillées « Ton schéma FAQ (JSON-LD) » et « Ton llms.txt ».

Il a raison et le diagnostic est plus large que les deux lignes : **le teaser vendait des noms de format, pas des bénéfices.** Le bloc « À publier » verrouillé est l'écran exact où un audit gratuit se transforme (ou pas) en 9 €/mois. Un fondateur DTC — l'ICP, sans budget agence et sans dev — ne sait pas ce qu'est un schéma JSON-LD. Un nom qu'il ne comprend pas ne peut pas lui donner envie de payer.

Réécriture des trois lignes machine (`publishTeaserItems`, `src/app/audit/[id]/report-insights.ts`), FR et EN, **bénéfice en titre, terme technique en fin de détail entre parenthèses** — pour l'agence ou le dev qui liront après lui, et parce que le contrat de page exige que ces fichiers restent nommés :

| avant | après |
|---|---|
| Ton schéma FAQ (JSON-LD) | **Ta FAQ dans le format que les IA lisent** — « Écrite depuis tes 6 questions d'achat auditées. Un bloc à coller une fois dans ton site, sans toucher au design (format technique : schéma FAQ JSON-LD). » |
| Ton llms.txt | **Ta fiche d'identité pour les assistants IA** — « Le fichier que ChatGPT et Gemini lisent en premier pour savoir ce que tu vends, à qui, et quand te recommander (llms.txt). » |
| Ton correctif robots.txt | **Le déblocage qui laisse les IA entrer sur ton site** — « GPTBot, ClaudeBot ne peuvent pas lire ton site aujourd'hui. Une ligne à corriger (robots.txt). » |

La première ligne du teaser (« la réponse rédigée à la question que tu perds ») était déjà en langage client : pas touchée.

**Piège évité.** Le contrat de page (`scripts/report-page-contract.test.ts`) impose deux choses contradictoires en apparence : le teaser doit contenir « FAQ », « llms.txt » et « robots.txt » (nommer ce qu'on livre), mais quand aucun crawler n'est bloqué **aucun `name` ne doit contenir « robots »**, et quand aucune question n'est perdue aucun `name` ne doit contenir « réponse ». Un premier jet naturel — « Tes réponses en version lisible par les IA » — aurait cassé le second test. Les noms retenus évitent les deux mots.

**Vérification.** `node_modules` **absent du dépôt** (ce n'est pas « installé pour macOS », c'est absent) : je n'ai pas installé dans le dépôt de Charles — un `npm ci` depuis Linux y remplacerait les binaires natifs macOS et casserait son `next dev`. Copie du dépôt dans le bac à sable (`/tmp/gp`), Node 23.11 arm64 officiel, `npm ci` : **`tsc --noEmit` 0 erreur**, **536 tests / 522 pass / 0 fail** (exactement la ligne de base du 11/09), **contrat de page 9/9**.

- 0 envoi, 0 dépense, 0 déploiement, 0 publication, 0 audit lancé, aucune page `/audit/` ouverte. Cloisonnement respecté.

## 5. Commande à l'agent produit

**Aucune.** Surveillance seule (Stripe → funnel → prod). Interdiction de lancer un lot, interdiction de `deploy_to_vercel` par upload. Après le push de Charles : contrôle croisé de l'instrument (un clic interne sous `gp_internal=1` sur un bouton de prix doit faire +1 sur `checkout_opened.internal`) **et** relecture à l'œil du bloc verrouillé sur un audit de contrôle — par `/api/audit-status`, jamais en ouvrant la page.

## 6. Préparé pour Charles

1. **Pousser** : `cd ~/Dev/Projects/getpick && git push origin main`. 7 commits — la caisse mesurable + ce correctif de copy. Vercel déploie seul. Front seul, réversible en un commit.
2. **Prospection : oui ou non.** Si oui, remettre `outbound/keys.env`.

## 7. Incapacités avec trace

- Push impossible depuis ici : aucun identifiant GitHub sur la machine (trace du 11/09, inchangée).
- `keys.env` absent → Instantly, signature des liens, attribution hors d'atteinte.
- `next build` complet : non tenté (binaire SWC Linux absent du dépôt copié ; `tsc` couvre le risque qui casse un déploiement).

## 8. Pièges appris ce run

- **`node_modules` absent ≠ `node_modules` pour macOS.** Le piège noté au dépôt disait « installés pour macOS » ; ici il n'y en avait aucun. Dans les deux cas la conclusion est la même — **on n'installe pas dans le dépôt de Charles**, on copie dans le bac à sable — mais la trace doit dire ce qu'on a vraiment vu.
- **Un test de contrat peut interdire un mot dans un champ tout en l'exigeant dans un autre.** Lire les *deux* assertions avant de réécrire un libellé, sinon la correction de copy casse le garde-fou anti-fuite.
- **Un verrou git orphelin peut dater d'un autre jour et d'une autre session.** `HEAD.lock` du 12/09 16:05 = la session produit bloquée. `mv` les deux verrous, pas seulement `index.lock`.
- **Une baisse de compteur sur fenêtre glissante n'est pas une régression.** `report_link_opened` 7 → 6 = une sortie par le bord, pas un événement perdu.

## 9. Pour le prochain run

1. `git ls-remote` d'abord : si `origin/main` ≥ `f5a70f9`, faire le contrôle croisé de l'instrument et fermer la dette de livraison.
2. Neutraliser les verrous git orphelins avant toute écriture (`mv`, jamais `rm`).
3. Même traitement à faire sur les libellés **payants** (`techJsonLdLabel`, `techLlmsLabel`, `techRobotsLabel` dans `src/lib/i18n.ts`) et sur la puce de prix « Fichiers machine prêts à installer » : même jargon, autre surface. Non fait ce run — le tier payant reçoit déjà le mode d'emploi par fichier, la douleur y est moindre.
4. Stripe avant funnel. Aucune dépense.
