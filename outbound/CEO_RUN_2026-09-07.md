# CEO_RUN — GetPick — lundi 7 septembre 2026

Relevé 08:12–08:35 UTC. Toutes les mesures de ce fichier viennent d'un appel réel.

---

## 0. Condition du run — la fausse incapacité des deux runs précédents est levée

Les runs du 05/09 et du 06/09 ont conclu « dépôt non monté, session non interactive ⇒ `request_cowork_directory` inutilisable ». **C'était faux, et personne ne l'avait essayé.**

Appel réel de ce run : `request_cowork_directory("~/Dev/Projects/getpick")` → *« Folder connected »*. Le dépôt est monté, complet, avec son historique git.

Correction de doctrine, à retenir : **un moyen ne se juge pas sur ce qu'on suppose de son contexte — on l'appelle, et c'est la réponse qui tranche.** C'est la loi « une clé ne se juge jamais sur son affichage », appliquée aux outils. Deux runs perdus.

---

## 1. Squads — relevé complet

| Point | État |
|---|---|
| Arbre de travail | **propre** (`git status --porcelain` vide) |
| Branche courante | `main`, **ahead 2** de `origin/main` |
| Commits non poussés | `38e2314`, `a3e9846` — **documentation de runs uniquement, aucun code** |
| `origin/main` | `1d338cb` — « fix(partage) : un lien de prospection expiré ouvre la porte de capture, jamais la caisse » |
| Production | **`1d338cb` déployé**, déploiement Vercel terminé 07/09 07:16:56Z |
| Branches `squad/*` | 21 branches distantes, toutes avec remote, **aucune branche locale orpheline** |
| Verrous FUSE | aucun |

**La dette de livraison est ZÉRO.** L'état « 15/08 : sept commits testés en attente sur `squad/2026-08-14-verdict-stabilite-rivaux` » a été recopié de run en run depuis trois semaines : ce travail est fusionné depuis longtemps (`21722c7 merge: gate des rapports, marquage trafic interne, stabilité des rivaux — tsc 0 erreur, 325 tests verts`). **Cette phrase est retirée du contexte permanent.**

### Push tenté — INCAPACITÉ avec trace
```
$ git push --dry-run origin main
fatal: could not read Username for 'https://github.com': No such device or address
```
Remote en HTTPS, aucun identifiant dans l'environnement d'exécution. Enjeu faible : les 2 commits sont des journaux, aucun code, aucun effet produit.

---

## 2. Métriques

### 2.1 Funnel — `GET https://www.getpick.ai/api/funnel`, 08:12 UTC, fenêtre 14 j

| Événement | Total | human | internal | bot | unknown |
|---|---|---|---|---|---|
| audit_started | 24 | 1 | 23 | 0 | 0 |
| audit_completed | 24 | 1 | 23 | 0 | 0 |
| report_viewed | 3 | 1 | 2 | 0 | 0 |
| report_link_opened | 7 | 0 | 7 | 0 | 0 |
| email_captured | 0 | 0 | 0 | 0 | 0 |
| teaser_cta_click | 0 | 0 | 0 | 0 | 0 |
| checkout_opened | 0 | 0 | 0 | 0 | 0 |
| followup_1_sent / _2_sent / _click | 0 | 0 | 0 | 0 | 0 |

`traffic_class_since = 2026-07-30T07:03:49.227Z`.

**Piège désamorcé** : fenêtre de classification 39 j > fenêtre 14 j ⇒ les zéros par classe sont des zéros *mesurés*. `unknown = 0` partout le confirme.

**Mouvement vs 06/09** : `audit_started` et `audit_completed` 25 → 24. Un événement est sorti par le bord de la fenêtre glissante — **vieillissement, pas baisse d'activité**. Tout le reste identique.

**Prévision retirée** : la baisse mécanique « −2/jour » annoncée au cockpit du 04/09 ne s'est pas produite. Elle n'est pas reconduite. Une prévision qui rate deux fois se retire, elle ne se réexplique pas.

**Attribution du `report_viewed` humain** : toujours impossible. Le mode détaillé exige l'en-tête `x-funnel-key` (valeur `FUNNEL_ADMIN_KEY`). Appel réel `?detail=1` sans clé : HTTP 200, **payload strictement identique à l'appel nu**, aucun champ d'attribution. ⇒ INCAPACITÉ, cause = fichier de clés absent (§2.4). **Ce 1 n'est pas compté comme un prospect** — le seul `report_viewed` humain d'août était l'équipe.

### 2.2 La caisse, lue à la source — FAIT NEUF DU RUN

Aucun run précédent n'avait interrogé Stripe. Le connecteur est disponible. Compte `acct_1TzBMoCZqJGb866f` (« Kinze »), **livemode**.

- `GET /v1/charges` → **liste vide**
- `GET /v1/subscriptions?status=all` → **liste vide**

⇒ **0 € encaissé, 0 abonnement, depuis l'origine du compte. Constat direct, plus une déduction depuis le funnel.**

`GET /v1/payment_links` — les deux liens de la home, tous deux `active: true` :

| Lien | Plan | Montant | Configuration |
|---|---|---|---|
| `plink_…PYGwZYIN` | `monitor_9eur` | 9 € / mois EUR | TVA auto, adresse requise, codes promo, confirmation hébergée FR |
| `plink_…WRHDaZrw` | `agent_19eur` | 19 € / mois EUR | idem |

La caisse est correctement montée. Ce n'est pas là que ça casse.

`GET /v1/checkout/sessions` — **4 sessions réelles**, toutes `expired` / `payment_status: unpaid` :

| Créée (UTC) | Plan | Montant | Devise présentée | Dans la fenêtre 14 j |
|---|---|---|---|---|
| 27/08 19:14 | Monitor | 9,00 € | CAD 15,11 | oui |
| 27/08 19:13 | Monitor | 9,00 € | MAD 100,88 | oui |
| 27/08 06:40 | Agent | 19,00 € | USD 23,00 | oui |
| 31/07 13:05 | Monitor | 9,00 € | EUR, TVA 1,50 € **calculée** | non |

### 2.3 Le compteur de caisse est aveugle — démonstration

Trois sessions Stripe réelles le 27/08 tombent **dans** la fenêtre 14 j. Le funnel affiche `checkout_opened = 0` sur cette même fenêtre. Les deux ne peuvent pas être vrais en même temps.

Cause trouvée par lecture du HTML **servi en production** (39 197 octets, User-Agent navigateur) :
- les deux boutons de prix sont des `<a href="https://buy.stripe.com/…">` **nus** ;
- `grep -c 'checkout_opened'` sur la page servie → **0** ;
- aucune occurrence de `teaser_cta_click`, aucun endpoint `/api/` appelé depuis la page de prix.

⇒ **`checkout_opened` ne peut structurellement jamais bouger depuis la home.** Ce n'est pas « personne n'a cliqué » : c'est « rien ne mesure les clics ». Les 3 sessions du 27/08 sont la preuve par l'absurde.

**Ce que ce fait NE dit PAS** — et c'est là que le piège attend : *le premier mouvement d'un compteur north star mérite plus de méfiance que son immobilité.*
- `customer_details` est **nul** sur les 4 sessions : aucune identité, aucune adresse email.
- Trois des quatre sont présentées en CAD, MAD et USD. La tarification adaptative de Stripe se règle sur l'IP du visiteur ⇒ **Canada, Maroc, États-Unis**. Les deux prospects écrits le 17/08 sont français. **Ces trois sessions ne sont pas attribuables à la prospection**, et rien ne permet de dire qu'il s'agit d'humains acheteurs plutôt que de scanners.
- La session du 31/07 est la seule en euros avec une TVA de 1,50 € effectivement calculée, ce qui suppose une adresse de facturation UE saisie dans le formulaire. **C'est le signal d'achat le plus avancé de l'histoire de GetPick.** Elle est hors fenêtre et non attribuable elle aussi.

**Conclusion de gouvernance, effective aujourd'hui : le revenu et l'ouverture de caisse se lisent chez Stripe. Le funnel garde l'amont — audit, rapport, capture d'email — et perd la juridiction sur la caisse jusqu'à réparation.**

### 2.4 Fichier de clés — absent, confirmé

`ls outbound/*.env*`, `ls .env*`, puis `find` sur l'ensemble des montages (`-name "*.env*"`, profondeur 4) : **aucun fichier**. Concordant avec le run produit du 07/09.

Ce fichier unique porte : `AUDIT_SHARE_SECRET` (signature des liens de partage — `src/lib/audit-share-token.ts` l.59 lève explicitement sans lui), les clés des moteurs d'audit, la clé API Instantly, et `FUNNEL_ADMIN_KEY`. **Un seul fichier manquant coupe quatre capacités.**

### 2.5 Outbound — Gmail, périmètre cloisonné

Requête unique : `getpick OR "getpick.ai" newer_than:25d`. Aucun balayage de `in:sent`, aucune lecture hors périmètre GetPick.

| Date | Objet | Cible | Réponse |
|---|---|---|---|
| 17/08 12:38 | absence des 12 réponses Gemini — compléments bio | vecteurenergy.bio | aucune |
| 17/08 12:38 | absente des 12 réponses, rival cité 8 fois — cosmétique bio | lafare1789.com | aucune |
| 28/08 12:13 | test d'acheminement SMTP `hello@getpick.ai` | interne | arrivé |

**21 jours sans envoi de prospection.** Le canal est vivant (prouvé le 28/08) : l'arrêt n'est pas une panne.

**Un lot de 2 ne mesure rien.** On n'a aucune donnée sur la qualité de la séquence actuelle. Écrire « la séquence ne marche pas » serait inventer.

### 2.6 Fichier de cibles

`outbound/sdr_prospects.csv` : 15 marques, colonnes `email` et `first_name` **vides**. Non exploitable en l'état, indépendamment des clés. Aucun registre RGPD d'oppositions distinct trouvé dans le dépôt.

### 2.7 Instantly — INCAPACITÉ
Clé dans le fichier absent. Non interrogé.

### 2.8 Surfaces de production — appels réels, User-Agent navigateur, `Cookie: gp_internal=1`

`/` 200 · `/fr` 200 · `/vs` 200 · `/study` 200 · `/api/funnel` 200 · `/api/audit-status` sans paramètre 400 (refus attendu, route vivante) · lien caisse Monitor 200 · lien caisse Agent 200.

**Aucune page `/audit/` ouverte** — elle émettrait un `report_viewed` et polluerait la north star.

Correction d'un relevé antérieur : le run du 06/09 notait `/fr`, `/vs`, `/study` comme absents ou non concluants à partir d'un test de `/pricing`, `/audit`, `/claim`, `/report`. Les quatre surfaces réelles répondent 200. Ce point est clos.

---

## 3. Diagnostic — un seul goulot

**Aucun prospect n'entre.** Troisième run consécutif, et le premier où la cause est nommée au lieu d'être supposée.

Ce n'est pas la friction : la caisse est montée, correcte, et jamais franchie ; la production est à jour et debout ; la dette produit est nulle ; le canal d'envoi est prouvé vivant. La friction a été réduite en août et n'est pas le facteur limitant.

Ce n'est pas non plus « l'agent produit ne travaille pas » : il a livré, mergé, et la prod sert son dernier commit.

**La cause matérielle est un fichier absent.** Sans lui, aucun lien de prospection ne peut être signé, donc aucun lot ne peut partir, donc aucun prospect ne peut entrer. C'est une chaîne, et elle est coupée au premier maillon. Ce maillon n'est pas à ma portée : le fichier n'existe nulle part sur les montages accessibles.

**La cause humaine reste ouverte.** La pause décidée le 14/08 pour les vacances n'a jamais été levée par écrit. Trois runs ont buté sur le même mur en cherchant chaque fois un obstacle technique de plus. La discipline dit : quand le même goulot revient et que la friction a déjà été réduite, on pose la question au lieu de deviner une fois de plus. **C'est la question posée aujourd'hui, une seule fois, sans liste.**

### Ce que le fait neuf du jour change — et ne change pas
Il **ne change pas** le goulot : instrumenter la caisse ne fait entrer aucun prospect.
Il **change** la fiabilité de ce qu'on regarde : la dernière marche avant le revenu n'était pas mesurée, et personne ne le savait. Le jour où un prospect arrive enfin, sans cette réparation on ne verrait pas s'il a touché la caisse.

---

## 4. COMMANDE À L'AGENT PRODUIT

**Priorité unique — rendre la caisse mesurable.** Émettre `checkout_opened` au clic sur chacun des deux boutons de prix, sur toutes les surfaces qui les portent, avec la classe de trafic déjà en place et l'étiquette de plan (`monitor_9eur` / `agent_19eur`).

**Compte comme livré :**
1. Un clic interne fait bouger `checkout_opened` au funnel, classé `internal` — vérifié funnel avant/après, **sans ouvrir aucune page `/audit/`**.
2. Le nombre lu chez Stripe et le nombre lu au funnel se recoupent sur la période de test. C'est ce contrôle croisé qui prouve l'instrument — un test unitaire vert ne prouve rien ici, puisque l'ancien code était vert en ne mesurant rien.
3. `npx tsc --noEmit` à zéro erreur avant de déclarer le lot prêt (le build type-vérifie `scripts/`, que ni les tests ni eslint ne voient).

**Hors périmètre :** toute nouvelle fonctionnalité, tout refactor, toute reprise du verdict concurrents, toute veille concurrentielle, toute action exigeant le fichier de clés.

**Raison du choix, et de l'ordre.** Le goulot est l'entrée de prospects et sa cause n'est pas à la portée de l'agent produit. Le meilleur travail débloqué est donc de réparer la seule mesure posée directement sur le revenu. On ne construit pas un instrument neuf : le compteur existe déjà côté serveur, il n'est simplement câblé nulle part côté page. Coût faible, effet permanent, aucune dépense.

---

## 5. Actions exécutées ce run

- Dépôt monté (appel réel), squads, branches, dette et commit servi en production relevés — dette remise à zéro et état périmé du 15/08 retiré.
- Funnel relevé, classes vérifiées contre `traffic_class_since`, mouvement 25 → 24 expliqué par le bord de fenêtre.
- **Caisse Stripe interrogée pour la première fois** : charges, abonnements, liens de paiement, sessions de paiement.
- **Défaut de mesure trouvé et prouvé** : `checkout_opened` structurellement aveugle, démontré par lecture du HTML servi et recoupement Stripe.
- Huit surfaces de production testées une à une ; correction d'un relevé erroné du 06/09.
- Outbound reconstitué en périmètre cloisonné ; fichier de cibles inspecté.
- Push tenté pour de vrai, échec tracé.
- Cockpit `getpick-cockpit-gtm-produit` réécrit.
- Commande à l'agent produit posée.

**Aucun envoi, aucune dépense, aucun déploiement, aucune publication, aucun audit lancé.**

---

## 6. Ce qui n'a pas été fait — une case chacun

| Point | Case | Détail |
|---|---|---|
| Reprise de la prospection | **INCAPACITÉ** | `AUDIT_SHARE_SECRET` absent ⇒ aucun lien signable (`audit-share-token.ts` l.59 lève). Trace : `find` sans résultat sur tous les montages. Geste : remettre `outbound/keys.env`. |
| Attribution du `report_viewed` humain | **INCAPACITÉ** | `FUNNEL_ADMIN_KEY` dans le même fichier. Trace : `?detail=1` sans clé, HTTP 200, payload identique à l'appel nu. |
| Instantly | **INCAPACITÉ** | Clé dans le même fichier. |
| Poussée des 2 commits de journal | **INCAPACITÉ** | Trace : `git push --dry-run` → `could not read Username for 'https://github.com'`. Aucun code concerné. |
| Lancement d'audits de contrôle | **DÉPENSE** | Chaque audit engage des appels payants aux moteurs. Non chiffrable au centime sans les clés. **Non proposé** : sans lien signable, un audit ne sert à rien. |
| Envoi d'un lot avec le fichier de cibles existant | **CHOIX** | 15 marques sans email ni prénom, aucun registre d'oppositions au dépôt. Envoyer sans vérifier oppositions et prénoms coûte plus que ne pas envoyer. Un email faux coûte plus qu'aucun email. |
| Correction du champ email « optionnel » | **CHOIX** | Défaut réel et chiffré (§g du cockpit), mais hors de la commande unique du jour. Une commande, pas trois. Reversé au backlog. |
| Veille concurrentielle | **CHOIX** | L'agent produit l'a faite le 05/09 et le 07/09. Ne pas refaire son travail. |

---

## 7. Dépenses proposées

**Aucune.**

---

## 8. Pour le prochain run

1. **Vérifier d'abord si le fichier de clés est revenu.** S'il est là : lancer immédiatement la vérification de l'aller-retour du lien signé contre la production (le piège du secret partagé coûte un lot entier s'il est découvert après l'envoi), puis attribuer le `report_viewed` humain.
2. **Ne plus jamais écrire une INCAPACITÉ d'outil sans avoir appelé l'outil.** Deux runs ont été perdus sur une supposition au sujet de `request_cowork_directory`.
3. **Lire la caisse chez Stripe à chaque run**, avant le funnel. C'est désormais la source du revenu.
4. Si la question posée aujourd'hui à Charles reste sans réponse au prochain run, ne pas la reposer : constater par écrit que la prospection est à l'arrêt par défaut de décision, et arbitrer en conséquence.
